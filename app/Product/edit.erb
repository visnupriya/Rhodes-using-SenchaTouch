<div data-role="page">

  <div data-role="header" data-position="inline">
    <h1>Edit <%= @product.name_sku_brand_price %></h1>
    <a href="<%= url_for :action => :show, :id => @product.object %>" class="ui-btn-left" data-icon="back" data-direction="reverse">
      Cancel
    </a>
    <a href="<%= url_for :action => :delete, :id => @product.object %>" class="ui-btn-right" data-icon="delete" data-direction="reverse">
      Delete
    </a>
  </div>

  <div data-role="content">
    <form method="POST" action="<%= url_for :action => :update %>">
      <input type="hidden" name="id" value="<%= @product.object %>"/>
      
          <div data-role="fieldcontain">
            <label for="product[name_sku_brand_price]" class="fieldLabel">Name sku brand price</label>
            <input type="text" id="product[name_sku_brand_price]" name="product[name_sku_brand_price]" value="<%= @product.name_sku_brand_price %>" <%= placeholder( "Name sku brand price" ) %> />
          </div>
      
      <input type="submit" value="Update"/>
    </form>
  </div>

</div>
